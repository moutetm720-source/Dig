--- src/services/trafficEngine.ts
+++ src/services/trafficEngine.ts
@@ -179,12 +179,9 @@
   }
 
   public getState(): TrafficEngineState {
-   private ensureInitialLiveTraffic() {
-    // DISABLED: User explicitly requested 'que du réel' (only real data).
-    return;
-  }= initialPool.length + Math.floor(Math.random() * 12);
-      }
-      this.saveState();
-    }
+    return this.state;
+  }
+
+  private ensureInitialLiveTraffic() {
+    // DISABLED: User explicitly requested 'que du réel' (only real data).
+    return;
   }
 
   /**
